export enum AppUrls {
    HOME = 'home',
    PROJECTS = 'projects',
    NOT_FOUND = '**'
}

export enum ProjectUrls {
    BOUNDEAD = 'boundead',
    REAL_DISUNITY = 'realdisunity',
    TESSERACT = 'tesseract',
    SHUTDOWN = 'shutdown',
    UNREAL_ENGINE_MATERIALS = 'uematerials'
}
